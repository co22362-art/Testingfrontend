/**
 * LICENSES CONNECTED
 * ─────────────────────────────────────────────────────────
 * Wrap Figma's `LicensesPage` component.
 * TODO: Wire to licenses API (pa_3021_license_management) when ready.
 */
import LicensesPage from '../../app/modules/3021_licenses/LicensesPage';

export default function LicensesConnected() {
    return <LicensesPage />;
}
