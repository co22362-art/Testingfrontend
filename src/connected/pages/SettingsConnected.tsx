/**
 * SETTINGS CONNECTED
 * ─────────────────────────────────────────────────────────
 * Wrap Figma's `SettingsPage` component.
 * TODO: Wire to settings API when available.
 */
import SettingsPage from '../../app/modules/3014_settings/SettingsPage';

export default function SettingsConnected() {
    return <SettingsPage />;
}
